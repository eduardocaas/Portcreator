import { CertificationSearchPipe } from './certification-search.pipe';

describe('CertificationSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CertificationSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
